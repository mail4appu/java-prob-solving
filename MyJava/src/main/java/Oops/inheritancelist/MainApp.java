package Oops.inheritancelist;

public class MainApp {
    /*public static void main(String[] args) {
        List<Animal> animals= new ArrayList<>();
        Animal animal= new Animal();
        animal.setName("Animal-1");
        animals.add(animal);
        List<Cat> cats= new ArrayList<>();
        Cat cat= new Cat();
        cat.setName("Cat-1");
        cat.setId("123");
        cats.add(cat);
        // Most important statement when actual method is not declared as playWithAnimals(List<? extends Animal> animals)
        animals=(List<Animal>)(List<?>)cats;
        playWithAnimals(animals);
        BeanUtils.copyProperties(animal,cat);
        System.out.println(cat);
    }

    public static void playWithAnimals(List<Animal> animals){
        animals.stream().forEach(i->i.scream());
    }*/
}
